// See https://aka.ms/new-console-template for more information
using CodeWarsTest.Katas;
using System.Collections;
using System.Linq;
using System.Numerics;


Console.WriteLine(MaximumSubarraySum.MaxSequence(new[] {-2, 1, -3, 4, 5, -1}));

/*List<TestClass> testClasses = 
    new List<TestClass>() { 
        new TestClass() { digit = 5},
        new TestClass() { digit = 1},
        new TestClass() { digit = 8}};
//testClasses[2] = testClasses[0] + testClasses[1];

foreach (var i in testClasses.Select(x => x).OrderBy(x => x))
    Console.WriteLine(i);
long a = new TestClass() { digit = 5 };

Console.WriteLine();
Console.WriteLine(testClasses[2][1]);

int[,] anime = new int[3, 4];
*/